# Wiki pages

Long-form docs, kept out of the README so it stays readable.

- [api/](api/) — the HTTP control API, endpoint by endpoint
- [Adding-a-Version.md](Adding-a-Version.md) — the generator runs a new
  Minecraft release needs

These are written as GitHub wiki pages and named accordingly. Once this repo is
public, the directory can be pushed to `understudy-client.wiki.git` unchanged
and the README links repointed at the wiki. Private wikis need a paid plan,
which is why they live here for now.
