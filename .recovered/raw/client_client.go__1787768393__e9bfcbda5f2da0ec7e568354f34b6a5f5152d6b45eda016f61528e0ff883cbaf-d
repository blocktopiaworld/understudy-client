//line /home/smoca/work/blocktopia/blocktopia-client/client/client.go:1:1
// Package client drives a Minecraft server connection far enough to be a real
// player: it completes the handshake, login and configuration states, enters
// play, and then keeps the connection healthy.
//
// It knows nothing about Blocktopia. Everything here is vanilla protocol
// behaviour, so the package stays reusable — the assertion layer that makes
// this a *test* harness lives outside it.
//
// # Concurrency
//
// A Client is safe for concurrent use once Connect returns. Exactly one
// goroutine may call Run; every other method may be called from any goroutine,
// which is what lets a control API drive the bot while the read loop pumps.
package client

import (
	"context"
	"errors"
	"fmt"
	"log/slog"
	"sync"
	"sync/atomic"
	"time"

	"github.com/blocktopia/client/protocol"
)

// Default timeouts, applied by New when Options leaves them zero.
const (
	// DefaultDialTimeout bounds the TCP connect.
	DefaultDialTimeout = 10 * time.Second
	// DefaultReadTimeout bounds any single read once connected. It must
	// comfortably exceed the server's keep-alive interval (vanilla: 15s) or a
	// healthy idle connection is torn down as if it had stalled.
	DefaultReadTimeout = 60 * time.Second
)

// Options configures a Client.
type Options struct {
	// Addr is the server's host:port.
	Addr string
	// Host and Port are what the handshake advertises. Servers may route on
	// these (virtual hosts, BungeeCord), so they are kept separate from the
	// address actually dialled. Defaults are derived from Addr.
	Host string
	Port uint16
	// Username is the offline-mode player name. The UUID is derived from it.
	Username string
	// Version is the Minecraft version to speak, e.g. "26.1". Empty means
	// auto-detect: the client pings the server first and adopts whatever
	// protocol it reports, which is what makes one binary usable against a
	// fleet of servers on different versions.
	Version string
	// DialTimeout bounds the TCP connect. Defaults to DefaultDialTimeout.
	DialTimeout time.Duration
	// ReadTimeout bounds any single read. Defaults to DefaultReadTimeout.
	ReadTimeout time.Duration

	// DisableAutoFall stops the bot falling by itself after a teleport.
	//
	// Auto-fall is on by default because teleporting is constant during tests
	// and vanilla kicks a floating player after about four seconds. A bot
	// teleported into mid-air and left there dies mid-suite with no warning,
	// which is a test failure that has nothing to do with the test.
	//
	// Falling after a teleport is also simply what a real client does. When the
	// bot is already on solid ground the fall detects that within a tick or two
	// and costs ~100ms, so leaving it on is nearly free.
	DisableAutoFall bool

	// DisableAutoRespawn leaves a killed bot on the death screen. Auto-respawn
	// is on by default (the zero value) because without it a dead bot is a
	// *silent* dead end: the connection stays healthy and keep-alives keep
	// flowing, but the server will not position a player who has not
	// respawned, so every later action is quietly ignored and the run simply
	// stops making progress. Bots die routinely — lava, falls, mobs — so a
	// harness wants respawn handling on. It is switchable because a test that
	// asserts on death itself needs to observe the corpse.
	DisableAutoRespawn bool

	// DisableIdlePosition stops the bot reporting its position while standing
	// still. See Client.startPositionLoop for why that is almost never what
	// you want.
	DisableIdlePosition bool

	// Logger receives connection lifecycle events. Defaults to slog.Default().
	Logger *slog.Logger

	// OnPacket, if set, observes every decoded clientbound packet.
	//
	// It is called synchronously on the read loop, so a slow callback delays
	// keep-alive replies and can get the bot kicked. Intended for tracing.
	OnPacket func(state protocol.State, p protocol.Packet)
}

// withDefaults fills in the zero-valued options and validates the rest.
func (o Options) withDefaults() (Options, error) {goCover_44b1aa629e35__8[0] = 18 ; goCover_44b1aa629e35__8[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__8[2] = 8 ; goCover_44b1aa629e35__8[3] = 1;
	if o.Addr == "" {goCover_44b1aa629e35__8[10] = 1;
		return o, errors.New("client: Addr is required")
	}
	goCover_44b1aa629e35__8[4] = 1;if o.Username == "" {goCover_44b1aa629e35__8[11] = 1;
		return o, errors.New("client: Username is required")
	}
	goCover_44b1aa629e35__8[5] = 1;if o.DialTimeout == 0 {goCover_44b1aa629e35__8[12] = 1;
		o.DialTimeout = DefaultDialTimeout
	}
	goCover_44b1aa629e35__8[6] = 1;if o.ReadTimeout == 0 {goCover_44b1aa629e35__8[13] = 1;
		o.ReadTimeout = DefaultReadTimeout
	}
	goCover_44b1aa629e35__8[7] = 1;if o.Logger == nil {goCover_44b1aa629e35__8[14] = 1;
		o.Logger = slog.Default()
	}
	goCover_44b1aa629e35__8[8] = 1;if o.Host == "" || o.Port == 0 {goCover_44b1aa629e35__8[15] = 1;
		host, port, err := splitHostPort(o.Addr)
		if err != nil {goCover_44b1aa629e35__8[18] = 1;
			return o, err
		}
		goCover_44b1aa629e35__8[16] = 1;if o.Host == "" {goCover_44b1aa629e35__8[19] = 1;
			o.Host = host
		}
		goCover_44b1aa629e35__8[17] = 1;if o.Port == 0 {goCover_44b1aa629e35__8[20] = 1;
			o.Port = port
		}
	}
	goCover_44b1aa629e35__8[9] = 1;return o, nil
}

// Position is the player's location and facing, as last known from the server.
type Position struct {
	X, Y, Z    float64
	Yaw, Pitch float32
}

// Client is a single bot connection. See the package comment for what may be
// called concurrently.
type Client struct {
	opts     Options
	log      *slog.Logger
	conn     *protocol.Conn
	entities *entityTracker
	world    *world
	inv      *inventory
	// v holds every version-specific detail: packet IDs, entity and block
	// tables, and the chunk framing flags. Resolved during Connect, since
	// auto-detection needs a round trip to the server, and read-only after.
	v *protocol.Version

	// UUID is the offline-mode UUID derived from Username. It is set before
	// connecting, because callers key assertions on it.
	UUID protocol.UUID

	// mu guards the mutable player state below.
	mu       sync.RWMutex
	state    protocol.State
	entityID int32
	pos      Position
	joined   bool
	health   float32
	food     int32
	dead     bool
	deaths   int
	onGround bool
	heldSlot int
	input    uint8
	// corrections counts server-issued position corrections. See the increment
	// site: it is one of the client's ground-detection signals.
	corrections int

	// falling guards auto-fall against re-entry. A fall provokes the very
	// position corrections that trigger auto-fall, so without this the bot
	// would spawn a new fall for every packet of its own descent.
	falling atomic.Bool

	// loadedSent guards the once-per-session player_loaded packet.
	loadedSent atomic.Bool

	// positionLoop guards the once-per-session idle position goroutine.
	positionLoop atomic.Bool

	// lastPositionSent is when a movement packet last went out, as Unix nanos.
	// The idle loop uses it to stay out of the way of a walk or a fall.
	lastPositionSent atomic.Int64

	// lastTeleportEcho is when the reply to a server teleport last went out, as
	// Unix nanos. See awaitTeleportSettle.
	lastTeleportEcho atomic.Int64

	// blockSequence numbers this connection's block actions. See nextSequence.
	blockSequence atomic.Int32

	// background goroutines (auto-fall, the idle position loop) are tracked so
	// Close can wait for them instead of letting them write to a closed socket.
	wg sync.WaitGroup
}

// New builds a Client. It does not perform any I/O.
func New(opts Options) (*Client, error) {goCover_44b1aa629e35__9[0] = 6 ; goCover_44b1aa629e35__9[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__9[2] = 9 ; goCover_44b1aa629e35__9[3] = 1;
	opts, err := opts.withDefaults()
	if err != nil {goCover_44b1aa629e35__9[6] = 1;
		return nil, err
	}

	goCover_44b1aa629e35__9[4] = 1;var version *protocol.Version
	if opts.Version != "" {goCover_44b1aa629e35__9[7] = 1;
		if version, err = protocol.ByName(opts.Version); err != nil {goCover_44b1aa629e35__9[8] = 1;
			return nil, err
		}
	}

	goCover_44b1aa629e35__9[5] = 1;return &Client{
		opts:     opts,
		v:        version,
		log:      opts.Logger.With("bot", opts.Username),
		UUID:     protocol.OfflineUUID(opts.Username),
		entities: newEntityTracker(),
		world:    newWorld(),
		inv:      newInventory(),
	}, nil
}

// Username returns the bot's player name.
func (c *Client) Username() string {goCover_44b1aa629e35__10[0] = 1 ; goCover_44b1aa629e35__10[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__10[2] = 10 ; goCover_44b1aa629e35__10[3] = 1; return c.opts.Username }

// Version returns the protocol version this client is speaking. It is nil
// until Connect has resolved it, which matters when auto-detecting.
func (c *Client) Version() *protocol.Version {goCover_44b1aa629e35__11[0] = 1 ; goCover_44b1aa629e35__11[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__11[2] = 11 ; goCover_44b1aa629e35__11[3] = 1; return c.v }

// background runs fn in a tracked goroutine, so Close can wait it out.
func (c *Client) background(fn func()) {goCover_44b1aa629e35__12[0] = 2 ; goCover_44b1aa629e35__12[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__12[2] = 12 ; goCover_44b1aa629e35__12[3] = 1;
	c.wg.Go(func() {goCover_44b1aa629e35__12[4] = 1;
		fn()
	})
}

// Close tears down the connection and waits for the client's own goroutines to
// finish, so a caller that returns straight after does not leave an auto-fall
// writing into a closed socket.
func (c *Client) Close() error {goCover_44b1aa629e35__13[0] = 3 ; goCover_44b1aa629e35__13[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__13[2] = 13 ; goCover_44b1aa629e35__13[3] = 1;
	var err error
	if c.conn != nil {goCover_44b1aa629e35__13[5] = 1;
		err = c.conn.Close()
	}
	goCover_44b1aa629e35__13[4] = 1;c.wg.Wait()
	return err
}

// Connect dials the server and drives handshake -> login -> configuration,
// returning once the client has entered the play state.
//
// It does not read play-state packets; call Run for that. Splitting the two
// lets a caller connect, assert on having joined, and only then start
// pumping — which keeps "did the bot get in?" separable from "what happened
// after it got in?".
//
// On failure the connection is closed before returning, so a caller that gives
// up after a failed Connect leaks nothing.
func (c *Client) Connect(ctx context.Context) (err error) {goCover_44b1aa629e35__14[0] = 14 ; goCover_44b1aa629e35__14[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__14[2] = 14 ; goCover_44b1aa629e35__14[3] = 1;
	if c.v == nil {goCover_44b1aa629e35__14[9] = 1;
		// Auto-detect. A server-list ping is a complete, separate connection
		// that needs no version agreement, so it is safe to make before we know
		// which protocol to speak.
		v, err := DetectVersion(c.opts.Addr, c.opts.Host, c.opts.Port, c.opts.DialTimeout)
		if err != nil {goCover_44b1aa629e35__14[11] = 1;
			return err
		}
		goCover_44b1aa629e35__14[10] = 1;c.v = v
		c.log.Info("auto-detected server version", "version", v.Name, "protocol", v.Protocol)
	}

	goCover_44b1aa629e35__14[4] = 1;conn, err := protocol.Dial(c.opts.Addr, c.opts.DialTimeout)
	if err != nil {goCover_44b1aa629e35__14[12] = 1;
		return err
	}
	goCover_44b1aa629e35__14[5] = 1;c.conn = conn
	defer func() {goCover_44b1aa629e35__14[13] = 1;
		if err != nil {goCover_44b1aa629e35__14[14] = 1;
			_ = c.conn.Close()
		}
	}()

	goCover_44b1aa629e35__14[6] = 1;c.setState(protocol.StateHandshaking)
	c.log.Info("connected", "addr", c.opts.Addr, "uuid", c.UUID.String())

	if err = c.handshake(); err != nil {goCover_44b1aa629e35__14[15] = 1;
		return err
	}
	goCover_44b1aa629e35__14[7] = 1;if err = c.login(ctx); err != nil {goCover_44b1aa629e35__14[16] = 1;
		return err
	}
	goCover_44b1aa629e35__14[8] = 1;return c.configure(ctx)
}

// Run pumps play-state packets until ctx is cancelled, the server disconnects,
// or a protocol error occurs. Exactly one goroutine may call it.
//
// Only the packets required to stay connected, to know where we are and to
// model the world get decoded; everything else is skipped by its length
// prefix. That is what keeps this client small — there are 141 clientbound
// play packets in 26.1 and this handles a couple of dozen.
func (c *Client) Run(ctx context.Context) error {goCover_44b1aa629e35__15[0] = 7 ; goCover_44b1aa629e35__15[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__15[2] = 15 ; goCover_44b1aa629e35__15[3] = 1;
	if c.State() != protocol.StatePlay {goCover_44b1aa629e35__15[5] = 1;
		return errors.New("client: Run called before entering play state")
	}
	goCover_44b1aa629e35__15[4] = 1;for {goCover_44b1aa629e35__15[6] = 1;
		p, err := c.readPacket(ctx)
		if err != nil {goCover_44b1aa629e35__15[8] = 1;
			return err
		}
		// An unhandled packet is simply skipped: it was framed by its length
		// prefix, so ignoring its body costs nothing and keeps the stream in
		// step. That is what lets this client decode a couple of dozen of the
		// 141 clientbound play packets and ignore the rest.
		goCover_44b1aa629e35__15[7] = 1;if _, err := c.dispatch(ctx, p); err != nil {goCover_44b1aa629e35__15[9] = 1;
			return err
		}
	}
}

// dispatch routes one play packet to the subsystem that owns it.
//
// The subsystem handlers come first and short-circuit: entity bookkeeping and
// chunk data are the high-volume traffic, and they are self-contained.
func (c *Client) dispatch(ctx context.Context, p protocol.Packet) (bool, error) {goCover_44b1aa629e35__16[0] = 4 ; goCover_44b1aa629e35__16[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__16[2] = 16 ; goCover_44b1aa629e35__16[3] = 1;
	for _, handle := range []func(protocol.Packet) (bool, error){
		c.handleEntityPacket,
		c.handleWorldPacket,
		c.handleInventoryPacket,
	} {goCover_44b1aa629e35__16[5] = 1;
		if handled, err := handle(p); err != nil || handled {goCover_44b1aa629e35__16[6] = 1;
			return handled, err
		}
	}
	goCover_44b1aa629e35__16[4] = 1;return c.handleSessionPacket(ctx, p)
}

// handleSessionPacket decodes the packets that keep the session alive and
// track the player themselves: joining, being moved, health, death and kicks.
func (c *Client) handleSessionPacket(ctx context.Context, p protocol.Packet) (bool, error) {goCover_44b1aa629e35__17[0] = 15 ; goCover_44b1aa629e35__17[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__17[2] = 17 ; goCover_44b1aa629e35__17[3] = 1;
	switch p.ID {
	case c.v.Packets.CBPlayLogin:goCover_44b1aa629e35__17[5] = 1;
		r := p.Reader()
		entityID := r.I32()
		if err := r.Err(); err != nil {goCover_44b1aa629e35__17[15] = 1;
			return true, err
		}
		goCover_44b1aa629e35__17[6] = 1;c.mu.Lock()
		c.entityID = entityID
		c.joined = true
		c.mu.Unlock()
		c.log.Info("joined the game", "entity_id", entityID)
		return true, nil

	case c.v.Packets.CBPlayPosition:goCover_44b1aa629e35__17[7] = 1;
		return true, c.handleTeleport(ctx, p)

	case c.v.Packets.CBPlayKeepAlive:goCover_44b1aa629e35__17[8] = 1;
		r := p.Reader()
		id := r.I64()
		if err := r.Err(); err != nil {goCover_44b1aa629e35__17[16] = 1;
			return true, err
		}
		goCover_44b1aa629e35__17[9] = 1;return true, c.conn.WritePacket(
			protocol.NewWriter(c.v.Packets.SBPlayKeepAlive).I64(id).Bytes())

	case c.v.Packets.CBPlayUpdateHealth:goCover_44b1aa629e35__17[10] = 1;
		r := p.Reader()
		health := r.F32()
		food := r.VarInt()
		if err := r.Err(); err != nil {goCover_44b1aa629e35__17[17] = 1;
			return true, err
		}
		goCover_44b1aa629e35__17[11] = 1;c.mu.Lock()
		c.health, c.food = health, food
		c.mu.Unlock()
		return true, nil

	case c.v.Packets.CBPlayDeathCombatEvent:goCover_44b1aa629e35__17[12] = 1;
		return true, c.handleDeath()

	case c.v.Packets.CBPlayRespawn:goCover_44b1aa629e35__17[13] = 1;
		// The payload is a SpawnInfo block that this client does not need; the
		// authoritative position arrives separately as a teleport.
		c.mu.Lock()
		c.dead = false
		c.mu.Unlock()
		// A respawn can cross dimensions, and entity IDs do not survive that.
		// Keeping stale entries would let the bot attack an ID that now means
		// something else, or nothing.
		c.entities.reset()
		// Terrain is dimension-scoped too, and the same chunk coordinate means
		// different blocks on the other side of a portal.
		c.world.reset()
		c.log.Info("respawned")
		return true, nil

	case c.v.Packets.CBPlayKickDisconnect:goCover_44b1aa629e35__17[14] = 1;
		// The play-state kick reason is an NBT text component, not a
		// length-prefixed string. Reading it as one prints binary noise on the
		// single most important failure path, so pull the readable parts out
		// instead of pretending to decode NBT.
		return true, fmt.Errorf("client: kicked: %s", readableText(p.Data))
	}
	goCover_44b1aa629e35__17[4] = 1;return false, nil
}

// handleTeleport answers a server-issued teleport.
//
// The acknowledgement is not optional bookkeeping. Until the server sees both
// the confirm and the position echo it holds the player in an "awaiting
// position from client" state, resends the teleport, and — the part that bites
// — silently drops use_item, use_item_on and player_action. See
// awaitTeleportSettle.
func (c *Client) handleTeleport(ctx context.Context, p protocol.Packet) error {goCover_44b1aa629e35__18[0] = 7 ; goCover_44b1aa629e35__18[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__18[2] = 18 ; goCover_44b1aa629e35__18[3] = 1;
	r := p.Reader()
	teleportID := r.VarInt()
	x, y, z := r.F64(), r.F64(), r.F64()
	r.F64() // dx
	r.F64() // dy
	r.F64() // dz
	yaw, pitch := r.F32(), r.F32()
	if err := r.Err(); err != nil {goCover_44b1aa629e35__18[7] = 1;
		return err
	}

	goCover_44b1aa629e35__18[4] = 1;c.mu.Lock()
	c.pos = Position{X: x, Y: y, Z: z, Yaw: yaw, Pitch: pitch}
	// Assume the server put us somewhere solid. Claiming to be airborne on
	// every update is what makes a server treat a bot as flying, so the default
	// has to be on-ground rather than zero.
	c.onGround = true
	// Count server-issued corrections. This is a ground-truth signal: the
	// server rejects a move into solid terrain and snaps the player back, so a
	// correction arriving mid-descent means "you have landed".
	c.corrections++
	c.mu.Unlock()

	if err := c.conn.WritePacket(
		protocol.NewWriter(c.v.Packets.SBPlayTeleportConfirm).VarInt(teleportID).Bytes()); err != nil {goCover_44b1aa629e35__18[8] = 1;
		return err
	}
	// Echoing the position back is what the vanilla client does and what
	// settles the server's "has the player accepted?" check.
	goCover_44b1aa629e35__18[5] = 1;ack := protocol.NewWriter(c.v.Packets.SBPlayPositionLook).
		F64(x).F64(y).F64(z).
		F32(yaw).F32(pitch).
		U8(protocol.MovementOnGround)
	if err := c.conn.WritePacket(ack.Bytes()); err != nil {goCover_44b1aa629e35__18[9] = 1;
		return err
	}
	goCover_44b1aa629e35__18[6] = 1;c.markTeleportEcho()
	c.log.Info("position synced", "x", x, "y", y, "z", z)

	c.announceLoaded()
	// Only start reporting idle position once we know where we are; before the
	// first teleport there is no position to report.
	c.startPositionLoop(ctx)
	// A teleport may well have put the bot in mid-air. Settle it onto ground
	// before vanilla's four-second float timer kicks it.
	c.autoFall(ctx)
	return nil
}

// handleDeath records a death and, unless disabled, asks to respawn.
func (c *Client) handleDeath() error {goCover_44b1aa629e35__19[0] = 5 ; goCover_44b1aa629e35__19[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__19[2] = 19 ; goCover_44b1aa629e35__19[3] = 1;
	// The death message is a text component and is deliberately left unparsed —
	// decoding NBT text is a large dependency for a string this client never
	// asserts on.
	c.mu.Lock()
	c.dead = true
	c.deaths++
	deaths := c.deaths
	c.mu.Unlock()
	c.log.Info("died", "deaths", deaths)

	if c.opts.DisableAutoRespawn {goCover_44b1aa629e35__19[6] = 1;
		c.log.Warn("auto-respawn disabled; bot will stay dead and ignore further actions")
		return nil
	}
	// Until this is sent the player stays on the death screen: the server keeps
	// the connection alive but refuses to position them, so everything
	// afterwards silently does nothing.
	goCover_44b1aa629e35__19[4] = 1;respawn := protocol.NewWriter(c.v.Packets.SBPlayClientCommand).
		VarInt(protocol.ClientCommandPerformRespawn)
	if err := c.conn.WritePacket(respawn.Bytes()); err != nil {goCover_44b1aa629e35__19[7] = 1;
		return err
	}
	goCover_44b1aa629e35__19[5] = 1;c.log.Info("respawn requested")
	return nil
}

// readPacket reads one packet, honouring ctx and the read timeout.
func (c *Client) readPacket(ctx context.Context) (protocol.Packet, error) {goCover_44b1aa629e35__20[0] = 13 ; goCover_44b1aa629e35__20[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__20[2] = 20 ; goCover_44b1aa629e35__20[3] = 1;
	if err := ctx.Err(); err != nil {goCover_44b1aa629e35__20[9] = 1;
		return protocol.Packet{}, err
	}
	goCover_44b1aa629e35__20[4] = 1;deadline := time.Now().Add(c.opts.ReadTimeout)
	if d, ok := ctx.Deadline(); ok && d.Before(deadline) {goCover_44b1aa629e35__20[10] = 1;
		// Deliberately let the context deadline land first. Setting the socket
		// deadline to exactly the context deadline is a race: the i/o timeout
		// can surface before ctx.Err() is set, which reports a clean shutdown
		// as a read failure. The margin makes the context always win.
		deadline = d.Add(deadlineMargin)
	}
	goCover_44b1aa629e35__20[5] = 1;if err := c.conn.SetReadDeadline(deadline); err != nil {goCover_44b1aa629e35__20[11] = 1;
		return protocol.Packet{}, err
	}
	goCover_44b1aa629e35__20[6] = 1;p, err := c.conn.ReadPacket()
	if err != nil {goCover_44b1aa629e35__20[12] = 1;
		if ctxErr := ctx.Err(); ctxErr != nil {goCover_44b1aa629e35__20[14] = 1;
			return protocol.Packet{}, ctxErr
		}
		goCover_44b1aa629e35__20[13] = 1;return protocol.Packet{}, err
	}
	goCover_44b1aa629e35__20[7] = 1;if c.opts.OnPacket != nil {goCover_44b1aa629e35__20[15] = 1;
		c.opts.OnPacket(c.State(), p)
	}
	goCover_44b1aa629e35__20[8] = 1;return p, nil
}

// deadlineMargin keeps the socket deadline just behind the context deadline so
// a clean shutdown never surfaces as an i/o timeout.
const deadlineMargin = 100 * time.Millisecond

// announceLoaded tells the server the world has finished loading, once.
//
// A vanilla client sends this after its level renderer is ready, and the
// server gates parts of a player's interaction on having received it. Never
// sending it leaves the player permanently half-joined: the symptom is that
// the *first* block action of a session is silently dropped — no rejection,
// the block simply does not break — and everything after it works, because by
// then the server has settled the player some other way.
//
// Sent after the first position sync rather than immediately on entering play,
// which is the point a real client has something loaded to report.
func (c *Client) announceLoaded() {goCover_44b1aa629e35__21[0] = 7 ; goCover_44b1aa629e35__21[1] = goCover_44b1aa629e35_P ; goCover_44b1aa629e35__21[2] = 21 ; goCover_44b1aa629e35__21[3] = 1;
	if c.v.Packets.SBPlayPlayerLoaded == protocol.Absent {goCover_44b1aa629e35__21[7] = 1;
		return
	}
	goCover_44b1aa629e35__21[4] = 1;if !c.loadedSent.CompareAndSwap(false, true) {goCover_44b1aa629e35__21[8] = 1;
		return
	}
	goCover_44b1aa629e35__21[5] = 1;if err := c.conn.WritePacket(protocol.NewWriter(c.v.Packets.SBPlayPlayerLoaded).Bytes()); err != nil {goCover_44b1aa629e35__21[9] = 1;
		c.log.Warn("could not announce player loaded", "err", err)
		return
	}
	goCover_44b1aa629e35__21[6] = 1;c.log.Debug("announced player loaded")
}
